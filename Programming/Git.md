### Git command:
- Show log:
```
$ git log --oneline --all --graph
```
- Show commit:

Show commit content:
```bash
$ git show <commit_hash>
```
Cat to stdout:
```bash
$ git show <commit_hash> | cat
```

#git #command